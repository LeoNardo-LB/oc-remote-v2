# UI Polish 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 5 项 UI 优化：搜索文案简化、代码块复制按钮、表格自适应+内部滚动、搜索工具显示入参、Read/Edit 标题短文件名确认。

**Architecture:** 每项改动独立、互不影响，可并行执行。改动集中在 Markdown 渲染层（MarkdownContent.kt、MarkdownTable.kt）和工具卡片层（SearchToolCard.kt）以及字符串资源。

**Tech Stack:** Kotlin + Jetpack Compose + Material 3 + mikepenz/multiplatform-markdown-renderer v0.41.0 + highlights (dev.snipme)

---

## File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `app/src/main/res/values*/strings.xml` (15个文件) | 修改 | `tool_search_code` 文案简化 |
| `app/src/main/kotlin/.../markdown/MarkdownContent.kt` | 修改 | 代码块启用 `showHeader = true` |
| `app/src/main/kotlin/.../markdown/MarkdownTable.kt` | 修改 | 表格自适应填满 + 内部滚动 |
| `app/src/main/kotlin/.../tools/cards/SearchToolCard.kt` | 修改 | 展开后显示入参 |

---

### Task 1: 搜索代码文案简化

**Files:**
- Modify: `app/src/main/res/values/strings.xml` — 英文 `tool_search_code`
- Modify: `app/src/main/res/values-zh-rCN/strings.xml` — 中文
- Modify: `app/src/main/res/values-ja/strings.xml` — 日文
- Modify: `app/src/main/res/values-ko/strings.xml` — 韩文
- Modify: `app/src/main/res/values-de/strings.xml` — 德文
- Modify: `app/src/main/res/values-es/strings.xml` — 西班牙文
- Modify: `app/src/main/res/values-fr/strings.xml` — 法文
- Modify: `app/src/main/res/values-it/strings.xml` — 意大利文
- Modify: `app/src/main/res/values-pt-rBR/strings.xml` — 葡萄牙文
- Modify: `app/src/main/res/values-pl/strings.xml` — 波兰文
- Modify: `app/src/main/res/values-ru/strings.xml` — 俄文
- Modify: `app/src/main/res/values-tr/strings.xml` — 土耳其文
- Modify: `app/src/main/res/values-uk/strings.xml` — 乌克兰文
- Modify: `app/src/main/res/values-ar/strings.xml` — 阿拉伯文
- Modify: `app/src/main/res/values-id/strings.xml` — 印尼文

- [ ] **Step 1: 修改所有语言文件中的 `tool_search_code`**

将 `tool_search_code` 的值从带"code/代码"后缀的文案简化为纯"搜索"含义：

| 文件 | 旧值 | 新值 |
|------|------|------|
| `values/strings.xml` | `Search code` | `Search` |
| `values-zh-rCN/strings.xml` | `搜索代码` | `搜索` |
| `values-ja/strings.xml` | `コード検索` | `検索` |
| `values-ko/strings.xml` | `코드 검색` | `검색` |
| `values-de/strings.xml` | `Code durchsuchen` | `Suchen` |
| `values-es/strings.xml` | `Buscar código` | `Buscar` |
| `values-fr/strings.xml` | `Trouver des fichiers` | `Rechercher` |
| `values-it/strings.xml` | `Cerca codice` | `Cerca` |
| `values-pt-rBR/strings.xml` | `Pesquisar código` | `Pesquisar` |
| `values-pl/strings.xml` | `Przeszukaj kod` | `Szukaj` |
| `values-ru/strings.xml` | `Поиск кода` | `Поиск` |
| `values-tr/strings.xml` | `Kod ara` | `Ara` |
| `values-uk/strings.xml` | `Пошук коду` | `Пошук` |
| `values-ar/strings.xml` | `بحث في الكود` | `بحث` |
| `values-id/strings.xml` | `Cari kode` | `Cari` |

注意：`values-fr/strings.xml` 中 `tool_search_code` 当前值是 `Trouver des fichiers`（这个翻译本身就是错的），修正为 `Rechercher`（法语"搜索"）。

- [ ] **Step 2: 编译验证**

Run: `.\gradlew.bat compileBetaDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "refactor: 简化搜索代码工具文案（搜索代码→搜索）"
```

---

### Task 2: Markdown 代码块添加复制按钮

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/markdown/MarkdownContent.kt`

**API 参考（mikepenz v0.41.0 反编译确认）:**

```kotlin
// MarkdownHighlightedCodeBlock / MarkdownHighlightedCodeFence 签名:
@Composable
fun MarkdownHighlightedCodeBlock(
    content: String,
    node: ASTNode,
    style: TextStyle,
    highlightsBuilder: Highlights.Builder,
    showHeader: Boolean,  // ← 核心：true 启用语言标签+复制按钮
    wordWrap: Boolean
)

// highlightedCodeBlock / highlightedCodeFence 是预配置的 MarkdownComponent 实例（无 showHeader）
// 我们需要自定义 lambda 来传入 showHeader = true
```

- [ ] **Step 1: 在 MarkdownContent.kt 中添加 import**

在文件顶部 import 区域添加：

```kotlin
import com.mikepenz.markdown.compose.elements.MarkdownHighlightedCodeBlock
import com.mikepenz.markdown.compose.elements.MarkdownHighlightedCodeFence
import dev.snipme.highlights.Highlights
```

- [ ] **Step 2: 创建 highlightsBuilder 和自定义 codeBlock/codeFence**

在 `MarkdownContent` 函数体中，`val components = ...` 之前，添加：

```kotlin
val highlightsBuilder = remember { Highlights.Builder() }
```

将 `markdownComponents(...)` 中的代码块组件替换为自定义版本。**两个分支（wordWrap / 非 wordWrap）都需要改**：

> **关于参数名**：v0.41.0 最后一个 boolean 参数可能是 `wordWrap` 或 `immediate`（取决于构建产物）。如果编译报错"no value passed for parameter 'wordWrap'"，尝试改为命名参数 `wordWrap = false` 或 `immediate = false`。按位置传参时不受参数名影响。

> **关于无语言标签的代码块**：`showHeader = true` 时，mikepenz 对无语言标签的代码块（如 \`\`\`text 或纯 \`\`\`）默认显示 "CODE" 作为语言标签。这是框架内置行为，无需额外处理。

**非 wordWrap 分支**（原来用 `highlightedCodeBlock` / `highlightedCodeFence`）：

```kotlin
codeBlock = { model ->
    MarkdownHighlightedCodeBlock(
        content = model.content,
        node = model.node,
        style = typography.code,
        highlightsBuilder = highlightsBuilder,
        showHeader = true,
        wordWrap = false
    )
},
codeFence = { model ->
    MarkdownHighlightedCodeFence(
        content = model.content,
        node = model.node,
        style = typography.code,
        highlightsBuilder = highlightsBuilder,
        showHeader = true,
        wordWrap = false
    )
},
```

**wordWrap 分支**（原来没有 codeBlock/codeFence 自定义）：

```kotlin
codeBlock = { model ->
    MarkdownHighlightedCodeBlock(
        content = model.content,
        node = model.node,
        style = typography.code,
        highlightsBuilder = highlightsBuilder,
        showHeader = true,
        wordWrap = true
    )
},
codeFence = { model ->
    MarkdownHighlightedCodeFence(
        content = model.content,
        node = model.node,
        style = typography.code,
        highlightsBuilder = highlightsBuilder,
        showHeader = true,
        wordWrap = true
    )
},
```

- [ ] **Step 3: 移除旧的 import**

移除不再直接使用的 import（如果替换后不再引用）：

```kotlin
// 移除这两行（如果替换后不再使用）:
import com.mikepenz.markdown.compose.elements.highlightedCodeBlock
import com.mikepenz.markdown.compose.elements.highlightedCodeFence
```

- [ ] **Step 4: 编译验证**

Run: `.\gradlew.bat compileBetaDebugKotlin`
Expected: BUILD SUCCESSFUL

如果遇到 `Highlights.Builder` 相关编译错误，检查 `dev.snipme:highlights` 依赖是否已传递引入（通过 `multiplatform-markdown-renderer-code`）。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: Markdown代码块启用showHeader显示语言标签和复制按钮"
```

---

### Task 3: 表格自适应填满 + 内部滚动

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/markdown/MarkdownTable.kt`

**当前结构（需要改造的关键部分）：**

```kotlin
// 当前: 外层 Box 整体滚动
Box(
    modifier = Modifier
        .fillMaxWidth()
        .horizontalScroll(scrollState)   // ← 整个表格滚动（问题所在）
        .border(border, shape)
        .clip(shape)
) {
    SubcomposeLayout { constraints ->
        // ... 两遍测量，计算均匀列宽
        // totalWidth = colWidths.sum()  // ← 不保证填满父宽度
    }
}
```

- [ ] **Step 1: 改造外层为固定宽度容器 + 内部滚动**

将 `SimpleMarkdownTable` 中的外层 `Box` + `SubcomposeLayout` 改为三层结构。

**关键问题：** `horizontalScroll` 会向子组件传递 `maxWidth = Infinity`，导致 `SubcomposeLayout` 无法获取实际父容器宽度。解决方案：用 `onSizeChanged` 记录中间层实际宽度，在 SubcomposeLayout 中使用该宽度而非 `constraints.maxWidth`。

需要新增的 import（如果还没有）：

```kotlin
import androidx.compose.runtime.mutableIntStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.getValue
import androidx.compose.runtime.setValue
import androidx.compose.ui.layout.onSizeChanged
```

替换外层 `Box` + `SubcomposeLayout` 为：

```kotlin
Box(
    modifier = Modifier
        .fillMaxWidth()
        .border(border, shape)
        .clip(shape)
) {
    // 记录实际容器宽度（避免 horizontalScroll 传递 Infinity）
    var containerWidth by remember { mutableIntStateOf(0) }

    // 内部可滚动容器
    Box(
        modifier = Modifier
            .onSizeChanged { containerWidth = it.width }
            .horizontalScroll(scrollState)
    ) {
        SubcomposeLayout { constraints ->
            if (rows.isEmpty()) return@SubcomposeLayout layout(0, 0) {}

            // Phase 1: probe pass — 用宽松约束测量自然尺寸
            val looseConstraints = Constraints(
                minWidth = 0,
                maxWidth = constraints.maxWidth,  // Infinity from horizontalScroll
                minHeight = 0,
                maxHeight = constraints.maxHeight,
            )
            val probeMeasurables = subcompose("probe", cellContent)
            val probePlaceables = probeMeasurables.map { it.measure(looseConstraints) }

            // 计算每列最大宽度
            val colWidths = IntArray(columnCount) { 0 }
            probePlaceables.forEachIndexed { index, placeable ->
                val col = index % columnCount
                colWidths[col] = maxOf(colWidths[col], placeable.width)
            }

            // 填满策略：使用 containerWidth（实际测量值）而非 constraints.maxWidth（Infinity）
            val naturalWidth = colWidths.sum()
            val parentWidth = containerWidth
            val finalColWidths = if (naturalWidth > 0 && parentWidth > 0 && naturalWidth < parentWidth) {
                // 按比例拉伸填满，精确分配避免舍入误差
                val scale = parentWidth.toFloat() / naturalWidth.toFloat()
                val scaled = IntArray(columnCount) { col ->
                    (colWidths[col] * scale).toInt()
                }
                // 修正舍入误差：将差值分配到前 N 列
                val diff = parentWidth - scaled.sum()
                for (i in 0 until diff.coerceAtMost(columnCount)) {
                    scaled[i] += 1
                }
                scaled
            } else {
                colWidths
            }

            // Phase 2: final pass — 用最终列宽重新测量
            val finalMeasurables = subcompose("final", cellContent)
            val finalPlaceables = finalMeasurables.mapIndexed { index, measurable ->
                val col = index % columnCount
                val colConstraint = Constraints(
                    minWidth = finalColWidths[col],
                    maxWidth = finalColWidths[col],
                    minHeight = 0,
                    maxHeight = constraints.maxHeight,
                )
                measurable.measure(colConstraint)
            }

            // 计算行高
            val actualRowCount = rows.size
            val rowHeights = IntArray(actualRowCount) { 0 }
            finalPlaceables.forEachIndexed { index, placeable ->
                val row = index / columnCount
                rowHeights[row] = maxOf(rowHeights[row], placeable.height)
            }

            val totalWidth = finalColWidths.sum()
            val totalHeight = rowHeights.sum()

            layout(totalWidth, totalHeight) {
                var y = 0
                for (row in 0 until actualRowCount) {
                    var x = 0
                    for (col in 0 until columnCount) {
                        val idx = row * columnCount + col
                        if (idx < finalPlaceables.size) {
                            val p = finalPlaceables[idx]
                            p.placeRelative(x, y)
                        }
                        x += finalColWidths[col]
                    }
                    y += rowHeights[row]
                }
            }
        }
    }
}
```

核心变更：
1. 外层 `Box` 去掉 `.horizontalScroll()`，只保留 `.fillMaxWidth()` + `.border()` + `.clip()`
2. 新增中间 `Box` 带 `.horizontalScroll()`，用 `onSizeChanged` 记录实际宽度
3. `SubcomposeLayout` 使用 `containerWidth`（onSizeChanged 实际测量值）而非 `constraints.maxWidth`（Infinity）
4. 列宽拉伸使用精确分配算法，舍入误差分配到前 N 列，确保 `sum == parentWidth`

- [ ] **Step 2: 编译验证**

Run: `.\gradlew.bat compileBetaDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "fix: 表格自适应填满左右内边距+内容超宽时内部滚动"
```

---

### Task 4: 搜索工具展开后显示入参

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/tools/cards/SearchToolCard.kt`

**当前结构：** 展开后只有 Markdown output，不显示搜索参数。

- [ ] **Step 1: 在展开区域顶部添加入参信息块**

在 `ToolCardScaffold` 的 `expandedContent` lambda 中，`Surface`（Markdown 输出）之前，添加入参显示：

```kotlin
} {
    // 入参信息块
    if (pattern != null || !dirPath.isNullOrBlank()) {
        Surface(
            shape = RoundedCornerShape(4.dp),
            color = toolOutputContainerColor(isAmoled),
            border = if (isAmoled) BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.65f)) else null,
            modifier = Modifier.fillMaxWidth()
        ) {
            Column(
                modifier = Modifier.padding(horizontal = 8.dp, vertical = 4.dp),
                verticalArrangement = Arrangement.spacedBy(1.dp)
            ) {
                if (pattern != null) {
                    Text(
                        text = "pattern: $pattern",
                        style = CodeTypography.copy(
                            fontSize = 11.sp,
                            color = if (isAmoled) MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.85f) else MaterialTheme.colorScheme.onSecondaryContainer.copy(alpha = 0.8f)
                        ),
                        modifier = Modifier.codeHorizontalScroll()
                    )
                }
                if (!dirPath.isNullOrBlank()) {
                    Text(
                        text = "path: $dirPath",
                        style = CodeTypography.copy(
                            fontSize = 11.sp,
                            color = if (isAmoled) MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.85f) else MaterialTheme.colorScheme.onSecondaryContainer.copy(alpha = 0.8f)
                        ),
                        modifier = Modifier.codeHorizontalScroll()
                    )
                }
            }
        }
        Spacer(modifier = Modifier.height(4.dp))
    }

    // 原有的 Markdown 输出 Surface（保持不变）
    val halfScreenHeight = halfScreenHeight()
    // ...
```

需要添加的 import（如果还没有）：

```kotlin
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Spacer
import androidx.compose.foundation.layout.height
import androidx.compose.foundation.layout.Arrangement
import androidx.compose.material3.Text
import dev.minios.ocremote.ui.theme.CodeTypography
import dev.minios.ocremote.ui.screens.chat.util.codeHorizontalScroll
```

- [ ] **Step 2: 编译验证**

Run: `.\gradlew.bat compileBetaDebugKotlin`
Expected: BUILD SUCCESSFUL

如果 `codeHorizontalScroll` 不是现有扩展函数，替换为 `Modifier.horizontalScroll(rememberScrollState())`。

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: 搜索工具展开后显示pattern和path入参"
```

---

### Task 5: 构建验证 + UI 验收 + 发版

- [ ] **Step 1: 构建 Release APK**

Run: `.\gradlew.bat assembleBetaRelease`
Expected: BUILD SUCCESSFUL

- [ ] **Step 2: UI 验收（安装 Release APK 到设备后逐项确认）**

验证清单：
1. **搜索文案**：搜索工具标题显示 `"搜索 · pattern"` 而非 `"搜索代码 · pattern"`
2. **代码块复制按钮**：AI 回复中代码块顶部显示语言标签 + 复制按钮；点击复制按钮后可粘贴出代码内容；无语言标签的代码块也能正常显示
3. **表格自适应**：窄表格填满左右内边距；宽表格可仅在内部左右滑动；边框始终贴齐容器宽度
4. **搜索入参**：搜索工具展开后，结果上方显示 `pattern:` 和 `path:` 信息（path 为空时整行不显示）
5. **Read/Edit 标题**：确认显示短文件名而非全路径

- [ ] **Step 3: 推送并创建 Release**

```bash
git push origin master
gh release create v2.0.0-beta.115 --title "v2.0.0-beta.115" --notes "UI Polish: 代码块复制按钮+表格自适应+搜索文案简化+搜索入参显示"
gh release upload v2.0.0-beta.115 app\build\outputs\apk\beta\release\app-beta-release.apk
```
