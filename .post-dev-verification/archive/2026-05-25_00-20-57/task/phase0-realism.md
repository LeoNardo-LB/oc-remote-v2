# phase0-realism — 真实度决策

## 目标说明
确定测试执行的真实度级别。

## 综合分析
### 2026-05-25 00:15 — 真实度决策
> 前序节点 handoff（phase0-inventory）:
```markdown
项目零测试文件。所有验证手段为：
1. 构建验证 — 已通过 (BUILD SUCCESSFUL)
2. 静态代码审查 — 已完成（spec compliance review 14/14 ✅）
3. 模拟器运行时测试 — 需手动操作
```

**诚实评估**：

本项目是一个 Android 客户端应用（Kotlin + Compose），不提供可编程的服务端入口点。它的验证特点是：

1. **构建验证**：`gradlew assembleDebug` — 已通过 ✅
2. **自动化测试**：零测试文件，无法执行
3. **Instrumented 测试**：需要模拟器 + adb，当前项目没有 Compose UI 测试
4. **运行时验证**：需要安装 APK + 连接 OpenCode 服务端 + 手动触发权限请求

**状态**：OpenCode 服务端（Infrastructure）位于 http://10.0.2.2:8765（模拟器访问宿主机），但需要：
- 模拟器运行中（Medium_Phone API 37）
- 手动触发需要权限的操作
- 无法通过命令行自动触发 permission.asked 事件

**真实度级别决策**：
- L3 要求所有服务真实运行 + 生产等价部署 + 完整 E2E
- 本项目 Infrastructure (OpenCode 服务端) 可以在模拟器中真实连接
- 但无法自动化触发权限请求场景（需要 Agent 在服务端执行操作）
- 本次验证能做的真实级别：**构建 + 静态分析 + 已完成的代码审查**

## 任务清单
- [x] 真实度级别已确定：L3 不可行（无自动化测试入口），降级到构建验证 + 静态分析
- [x] 所有可用服务已确认连接：OpenCode 服务端 http://10.0.2.2:8765（需模拟器）
- [x] 执行级别公告已输出（见下方）
- [x] 无 BLOCKER — 但受限于项目无测试基础设施

## 任务总结
项目无测试基础设施，无法执行自动化测试验证。
已完成的验证：编译 ✅ + 静态代码审查 ✅
未完成：模拟器运行时测试（需手动）

## 路由
（非条件节点，无需填写）

## 创建的文件
（无）

## 修改的文件
（无）

## Issues
- 🔴 项目零测试基础设施，无法执行任何自动化测试
- 🟡 模拟器运行时验证需要手动操作，超出自动化验证范围
- 建议后续：建立 EventReducer/ViewModel 单元测试 + Compose UI 测试基础设施
