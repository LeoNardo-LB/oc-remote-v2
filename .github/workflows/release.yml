name: Build Release APK

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.0

      - name: Extract version from version.properties
        id: version
        run: |
          VERSION=$(grep 'VERSION_NAME=' version.properties | cut -d'=' -f2)
          VERSION_CODE=$(grep 'VERSION_CODE=' version.properties | cut -d'=' -f2)
          echo "name=$VERSION" >> $GITHUB_OUTPUT
          echo "code=$VERSION_CODE" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION (code: $VERSION_CODE)"

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Grant execute permissions for gradlew
        run: chmod +x ./gradlew

      - name: Remove local-only settings
        run: |
          sed -i '/^systemProp\./d' gradle.properties
          sed -i '/^org.gradle.java.home/d' gradle.properties

      - name: Decode Keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        if: ${{ env.KEYSTORE_BASE64 != '' }}
        run: |
          mkdir -p app/keystore
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > app/keystore/release.keystore
          echo "keystore=keystore/release.keystore" > app/keystore/signing.properties
          echo "keystore.alias=${{ secrets.KEYSTORE_ALIAS }}" >> app/keystore/signing.properties
          echo "keystore.password=${{ secrets.KEYSTORE_PASSWORD }}" >> app/keystore/signing.properties

      - name: Build Release APK
        run: ./gradlew :app:assembleBetaRelease

      - name: Rename APK
        run: |
          VERSION="${{ steps.version.outputs.name }}"
          mkdir -p release-apks
          # Find the built APK (signed or unsigned)
          APK=$(find app/build/outputs/apk/beta/release -name '*.apk' | head -1)
          if [ -n "$APK" ]; then
            cp "$APK" release-apks/oc-remote-plus-${VERSION}.apk
          else
            echo "::error::No APK found in app/build/outputs/apk/beta/release/"
            exit 1
          fi

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: ocremoteplus-release-${{ steps.version.outputs.name }}
          path: release-apks/*.apk
          retention-days: 90

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${{ steps.version.outputs.name }}"
          TAG="v${VERSION}"

          # Create tag if it doesn't exist
          git tag $TAG || true
          git push origin $TAG || true

          # Check if release already exists
          if gh release view $TAG > /dev/null 2>&1; then
            echo "Release $TAG already exists, uploading APK..."
            gh release upload $TAG release-apks/*.apk --clobber
          else
            echo "Creating new release $TAG..."
            if [ -f "RELEASE_NOTES_${VERSION}.md" ]; then
              gh release create $TAG \
                --title "${VERSION}" \
                --notes-file "RELEASE_NOTES_${VERSION}.md" \
                release-apks/*.apk
            else
              gh release create $TAG \
                --title "${VERSION}" \
                --generate-notes \
                release-apks/*.apk
            fi
          fi
