appId: dev.leonardo.ocremoteplus.dev
name: P0 Enter Session and Verify
---
# Tap on the session with text "Working" - this is an active session
- tapOn: "Working"

# Wait for chat screen to load
- extendedWaitUntil:
    timeout: 10000
    visible: ".*"
