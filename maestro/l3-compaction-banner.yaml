appId: dev.minios.ocremote.dev
name: L3 - Compaction Banner
tags:
  - layer3
  - manual
---
# Manual: Requires server connected, session with large context triggering compaction

- launchApp
- extendedWaitUntil:
    visible: ".*"
    timeout: 10000
- takeScreenshot: l3_compaction
# CompactionBanner appears when "Compressing context..." is active
